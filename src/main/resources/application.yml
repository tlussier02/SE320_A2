spring:
  datasource:
    url: jdbc:h2:file:./data/dta
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  h2:
    console:
      enabled: true

server:
  port: 8080
